apiVersion: v1
kind: Service
metadata:
  name: milibrary-api-service
  namespace: milibrary
spec:
  selector:
    app: milibrary-api
  type: LoadBalancer 
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
