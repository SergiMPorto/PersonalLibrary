apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  namespace: milibrary
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /data/postgres
