<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:contex=".MyLibraryActivity">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📚 Mi Biblioteca"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/teal_700"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Estadísticas de la biblioteca -->
    <TextView
        android:id="@+id/textViewStats"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="📚 0 libros | ✍️ 0 autores | 🌍 0 idiomas"
        android:textSize="14sp"
        android:textColor="@color/teal_200"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewTitle" />

    <!-- Campo de búsqueda -->
    <EditText
        android:id="@+id/editTextSearchMyLibrary"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="8dp"
        android:hint="Buscar en mi biblioteca..."
        android:inputType="text"
        android:drawableStart="@android:drawable/ic_menu_search"
        android:drawablePadding="8dp"
        android:padding="12dp"
        android:background="@drawable/edittext_background"
        app:layout_constraintEnd_toStartOf="@id/buttonSearchMyLibrary"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textViewStats" />

    <!-- Botón de búsqueda -->
    <Button
        android:id="@+id/buttonSearchMyLibrary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:text="Buscar"
        android:textSize="12sp"
        android:paddingHorizontal="16dp"
        app:layout_constraintBottom_toBottomOf="@id/editTextSearchMyLibrary"
        app:layout_constraintEnd_toStartOf="@id/buttonShowAll"
        app:layout_constraintTop_toTopOf="@id/editTextSearchMyLibrary" />

    <!-- Botón mostrar todos -->
    <Button
        android:id="@+id/buttonShowAll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Todos"
        android:textSize="12sp"
        android:paddingHorizontal="16dp"
        android:backgroundTint="@color/teal_200"
        app:layout_constraintBottom_toBottomOf="@id/editTextSearchMyLibrary"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/editTextSearchMyLibrary" />

    <!-- SwipeRefreshLayout que contiene el RecyclerView -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshLayout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/editTextSearchMyLibrary">

        <!-- RecyclerView para mostrar los libros -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerViewMyLibrary"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scrollbars="vertical"
            tools:listitem="@layout/item_saved_book" />

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

    <!-- Indicador de carga -->
    <ProgressBar
        android:id="@+id/progressBarMyLibrary"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/editTextSearchMyLibrary" />

    <!-- Mensaje cuando la lista está vacía -->
    <TextView
        android:id="@+id/textViewEmpty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="📚 Tu biblioteca está vacía\n\n¡Comienza agregando algunos libros!"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@color/teal_200"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/editTextSearchMyLibrary" />
</androidx.constraintlayout.widget.ConstraintLayout>
